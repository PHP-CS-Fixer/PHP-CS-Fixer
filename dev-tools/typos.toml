[default]
extend-ignore-re = [
    "@author .*",
    "(feature|minor) #[0-9]+ .+ (.*)",
]

extend-ignore-words-re = [
    "Writeable",
    "writeable",
]

[files]
extend-exclude = [
    "tests/**/*.*",
]
